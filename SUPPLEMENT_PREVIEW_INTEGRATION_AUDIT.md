# v4.6 補講プレビュー統合監査

**対象ブランチ**：`agent/start-v42-supplements`  
**対象PR**：#31  
**状態**：ドラフトPR内で統合プレビュー可能・未マージ・公開承認前

## 1. 今回到達した状態

補講A〜Hの教材データ、説明、対照表、問題、別進捗UIを、既存のPART 0＋第1〜20課へ直接混入させず、補講専用のプレビュー境界から利用できるようにした。

- 本課正本：PART 0＋第1〜20課
- 補講：A〜Hの別レイヤー
- 補講の番号：A〜Hを維持し、第21〜28課へ改称しない
- 本課保存キー：`tamil-verb-engine-v2`
- 補講保存キー：`tamil-supplements-v42`
- 本課URL：従来どおり `#ch=...`
- 補講URL：`#supp=hub`、`#supp=A&view=read` 等
- PR状態：ドラフト・未マージ

## 2. 読み込み境界

`index.html` は個々の補講ファイルへ直接結合せず、`supplement-preview-loader-v46.js` だけを読み込む。

ローダーは次を順番に読み込む。

1. 補講A〜Hカタログ
2. 補講A〜H教材データ
3. 補講H説明オーバーレイ
4. 補講別進捗モデル
5. 補講描画・音声ランタイム

途中で失敗した場合は補講入口を無効化して短く通知する。本課20課の読み込み・描画・進捗・音声は補講ローダーより前に完了しているため、補講だけの失敗で本課を利用不能にしない。

## 3. 補講画面

左上メニューへ「補講A〜Hを開く」を追加した。

補講ハブでは次を表示する。

- 補講コード・題名・入口の問い
- 前提となる本課
- 補講ごとの5段階進捗
- 全補講の合計進捗
- 機械検査済み・ネイティブ音声監査待ちの注記

各補講は次の4画面を持つ。

1. 読む
2. 聞く
3. 形
4. 練習

入口と出口に「先に学ぶ本課」「補講後に戻る」を置き、第1〜20課だけへ接続する。

## 4. 音声

補講は本課と別の音声ドックを使う。

- `ttsText` のタミル文字だけを渡す
- `utterance.lang = ta-IN`
- Tamil音声を優先
- Tamil音声が見つからない場合は短く通知し、既定音声で試す
- カード領域全体のタップで一回再生
- Enter／Spaceで一回再生
- 前／次、停止、通常／低速、1回／2回
- 全例文の連続再生
- 文間に短い待機を置く
- 再生中のカードを表示する

端末TTSは発音・韻律・自然さの証拠には使わない。

## 5. iPhone縦画面

`styles-supplements-v46.css` にスマートフォン用レイアウトを追加した。

- 520px以下で一列表示
- タミル文字を本文より大きく表示
- カナ12px以上
- 操作領域44〜48px以上
- 下部ナビで `safe-area-inset-bottom` を使用
- 音声ドックを画面下部ナビの上へ配置
- 横幅の狭い画面ではクイズ結果・前後移動を縦方向へ再配置

これはJSDOMとCSS静的検査による確認であり、実機の文字折返し、TTS音声選択、スクロール感、ブラウザ固有挙動の最終確認ではない。

## 6. レジスター表示

通常の補講生成対象は `SST / TN-GENERAL` とする。

補講Hの次の受け身は、主表示の口語形へ昇格させない。

- `கதவு திறக்கப்பட்டது.`
- `கடிதம் எழுதப்பட்டது.`

これらは次の属性を持つ。

- `primaryRegister: LT-WR`
- `styleTags: [formal, reading-only]`
- 画面表示：`LT-WR・読解`

`திறந்துடுச்சு` のような状態変化を文法的受け身とは扱わない。

## 7. 自動検査済み

CIの `Validate curriculum` を層別ステップに分け、次を個別に確認する。

- 公開PART 0＋第1〜20課のデータ
- 補講A〜Hの教材構造
- 補講E・F・G・Hの独立言語検査
- 補講A〜Hの進捗分離
- 補講統合プレビュー
- 既存DOM・設定移行・音声

統合プレビュー検査では次を確認する。

- A〜Hハブの描画
- 第21課以降へ改称していないこと
- URLルーティング
- 本課への復帰
- 本課保存領域を変更しないこと
- カード一回タップにつき一発話
- キーボード一操作につき一発話
- Tamil文字だけのTTS
- 連続再生と文間待機
- TTS API不在時の通知
- SST／LT-WR表示
- iPhone縦画面用CSS
- 12pxカナ
- safe area

## 8. 自動検査では承認しない項目

次は人間のネイティブ音声・文体確認が必要である。

- 発音・韻律・自然な文間休止
- タミル・ナードゥ州内で地域横断的に自然か
- 現代チェンナイで中立的か、局地的・俗語的か
- 発音ローマ字とカナの妥当性
- 候補となる自然な別形
- SST／LT-WR／くだけた形の文体境界
- 文脈を置いた時の意味・含意
- iPhone実機のTTS音声と操作感

## 9. 公開ゲート

補講を公開承認済みにする前に、次を満たす。

- `SUPPLEMENT_NATIVE_REVIEW_QUEUE.md` の高優先項目を確認
- 修正が必要な例文・発音・カナを反映
- `confidence` と `review.nativeReviewed` を個別更新
- D評価を公開対象へ含めない
- C評価には注記またはレビュー結果を残す
- iPhone縦画面で全補講を確認
- カード全面タップ、二重再生防止、連続再生を実機確認
- Tamil音声がない端末で停止せず通知することを確認
- PRをドラフトから外す判断を人間が行う

現時点では、統合プレビューは利用可能だが、言語上の公開承認とマージは行わない。